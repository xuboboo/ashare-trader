{
  "name": "ashare-trader",
  "version": "0.1.0",
  "description": "A 股 T+1 决策台：本地概率模型 / Jev / 规则三路决策 + 严格成本回测 + 人工执行回填",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "fetch:daily": "bun run scripts/fetch-daily.ts",
    "train": "bun run scripts/train-model.ts",
    "backtest": "bun run scripts/backtest.ts",
    "fill": "bun run scripts/fill.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@ai-sdk/typesafe-ai": "^3.0.0",
    "ai": "^7.0.103"
  }
}
